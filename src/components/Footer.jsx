// Footer component visible on every page (placed in Layout)
export default function Footer() {
  return (
    <footer>
      <small>© {new Date().getFullYear()} Your Name — Built with React & Vite</small>
    </footer>
  )
}
